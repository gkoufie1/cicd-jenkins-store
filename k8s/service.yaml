apiVersion: v1
kind: Service
metadata:
  name: storetrack
  namespace: storetrack
  labels:
    app.kubernetes.io/name: storetrack
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: storetrack
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
